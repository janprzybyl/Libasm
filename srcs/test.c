#include "../includes/Libasm.h"
#include <stdio.h>

int main()
{
    // if (ft_isdigit(22) == 1)
    //     printf("It is digit\n");
    // else
    //     printf("It is not digit\n");

    if (ft_isdigit('a') == 1)
        printf("It is alpha\n");
    else
        printf("It is not alpha\n");
    

    return (0);
}